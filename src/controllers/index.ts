import welcomeController from "./welcomeController.js";

// TODO тут теж може бути прописаний тип const routes: Router[], щоб зафіксувати конкретний тип за цим, тоді в тебе буде виникати помилка якщо ти в цей масив заженеш не клас Router

const routes = [welcomeController];

export default routes;
